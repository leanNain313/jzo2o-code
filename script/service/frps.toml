bindPort = 7000

# TLS（新版写法）
transport.tls.force = true

# 鉴权
auth.method = "token"
auth.token = "Yye02408926."

# 管理后台（可选）
webServer.addr = "0.0.0.0"
webServer.port = 7500
webServer.user = "admin"
webServer.password = "admin"